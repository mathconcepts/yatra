{
  "name": "yatra-bhuvan-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies Bhuvan tiles with CORS + HTML-200 error detection. Backs the v3.0 TileSourceChain fallback.",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  }
}
